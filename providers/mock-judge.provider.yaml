apiVersion: promptkit.altairalabs.ai/v1alpha1
kind: Provider
metadata:
  name: mock-judge
spec:
  id: mock-judge
  type: mock
  model: judge-model
  additional_config:
    mock_config: mock-responses-judge.yaml
  defaults:
    temperature: 0.0
    max_tokens: 64
    top_p: 1.0
